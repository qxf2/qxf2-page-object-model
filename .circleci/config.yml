version: 2

jobs:

  # using tox
  toxify:

      docker:
        - image: circleci/python:3.7.0

      steps:

        - checkout

        - run: pip install tox && tox

workflows:
  version: 2

  myproj:
    jobs:
      - toxify